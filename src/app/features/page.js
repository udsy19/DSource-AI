import FeaturesModes from "@/components/features/FeaturesModes";

export const metadata = {
  title: "Features — DSource.AI",
  description:
    "Everything DSource does, end to end: AI room render, material finder, mood boards, image-to-CAD, spec builder, the digital library, and folios. Watch the narrated story or browse the catalog.",
};

export default function FeaturesPage() {
  return <FeaturesModes />;
}
